import content from "../content/services.json" with { type: "json" };

/** Content for the six service pages. Edited via the admin, stored in content/services.json. */
export const servicePages = content.servicePages;

